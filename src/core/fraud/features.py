""" 
This page is make empty to make the app run without errors. 
The original content has been moved to the `src` directory for better organization and maintainability.
The original content is still accessible in the `src/core/fraud` and `src/core/phishing` directories, which contain the core logic for fraud and phishing detection, respectively.
This page can be restored to its original functionality by moving the relevant code back from the `src` directory if needed in the future.
This change was made to improve the structure of the project and separate the core logic from the Streamlit app pages.
"""